<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Onikayo 💘</title>

<style>
body{
  margin:0;
  font-family:Arial, sans-serif;
  display:flex;
  justify-content:center;
  align-items:center;
  height:100vh;
  background:linear-gradient(135deg,#ff4d6d,#c9184a);
  color:white;
  text-align:center;
}

.card{
  background:rgba(0,0,0,0.3);
  padding:40px;
  border-radius:20px;
  box-shadow:0 15px 40px rgba(0,0,0,0.4);
  max-width:500px;
}

h1{
  font-size:32px;
  margin-bottom:10px;
}

p{
  font-size:18px;
  margin-bottom:20px;
}

button{
  padding:12px 20px;
  font-size:16px;
  border:none;
  border-radius:10px;
  cursor:pointer;
  margin:5px;
  transition:0.3s;
}

.yes{
  background:#06d6a0;
  color:black;
}

.no{
  background:white;
  color:black;
}

button:hover{
  transform:scale(1.05);
}

.result{
  margin-top:20px;
  font-size:18px;
  font-weight:bold;
}
</style>
</head>

<body>

<div class="card">
  <h1>Onikayo ❤️</h1>
  <p>From the moment I met you, everything changed in the best way.</p>
  <p>Will you be my Valentine? 💘</p>

  <button class="yes" onclick="sayYes()">YES 💞</button>
  <button class="no" onclick="moveButton(this)">NO 🙈</button>

  <div class="result" id="result"></div>
</div>

<script>
function sayYes(){
  document.getElementById("result").innerHTML =
  "You just made Liyema the happiest man alive ❤️🌹";
}

function moveButton(btn){
  btn.style.position="absolute";
  btn.style.top=Math.random()*80+"%";
  btn.style.left=Math.random()*80+"%";
}
</script>

</body>
</html>
